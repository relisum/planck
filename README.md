# YOUR PERSONAL TRACKER
## Fast start:
1. run `yarn install-deps`
2. run `yarn prisma migrate dev --name init`
3. run `yarn prisma generate` in project root directory
4. add `.env` file in `client/` with `VITE_API_URL`
5. run `start.sh` from project root directory
